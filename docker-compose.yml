version: '3'
services:
  webapp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: webapp
    ports:
      - 8080:8080
    environment:
      - ENV1=THANKS
      - ENV2="FOR ALL"
      - ENV3="THE FISH"
